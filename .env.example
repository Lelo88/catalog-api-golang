# Puerto donde corre la API (go run ./cmd/api)
PORT=8080

# URL de conexión a PostgreSQL para desarrollo local.
# Formato estándar compatible con muchos drivers:
# postgres://USER:PASSWORD@HOST:PORT/DBNAME?sslmode=disable
DATABASE_URL=postgres://catalog:catalog@localhost:5432/catalog_db?sslmode=disable
