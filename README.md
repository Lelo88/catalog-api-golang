# catalog-api-golang
Diseño de catalogo backend en lenguaje Golang

## Entorno local (PostgreSQL)

Levantar PostgreSQL con Docker Compose:

```bash
docker compose up -d

## Migraciones

Instalar `migrate` (golang-migrate CLI):
- macOS: `brew install golang-migrate`

Levantar PostgreSQL:
```bash
docker compose up -d
